# Backend base URL (see server.servlet.context-path in application.yml).
# Copy this file to .env.local and adjust if the backend runs elsewhere.
VITE_API_BASE_URL=http://localhost:8080/cdr-generator/api/cdr
